using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Klak.Motion;
using Klak.Math;


public class SosakeiController
{
    List<SmoothFollow> smoothFollows;

    // Start is called before the first frame update
    void Start()
    {
        /*
        this.smoothFollows = new List<SmoothFollow>();
        for(int i = 0; i < 10; i++) {
            SmoothFollow sf = new SmoothFollow()
            //sf.target = 
            this.smoothFollows.Add(sf);
        }
        */
    }


    // Update is called once per frame
    void Update()
    {
    }

}
